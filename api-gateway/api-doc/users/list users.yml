info:
  name: list users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/users?pagination.page_size=100&pagination.page_token="
  params:
    - name: pagination.page_size
      value: "100"
      type: query
    - name: pagination.page_token
      value: ""
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
