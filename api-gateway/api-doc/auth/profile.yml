info:
  name: profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/auth/profile"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
