info:
  name: validate-token
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/validate-reset-password"
  body:
    type: json
    data: |-
      {
        "token": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
