info:
  name: reset-password
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "token": "test",
        "new_password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
